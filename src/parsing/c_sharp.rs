
use std::str::Chars;

use renounce::*;
use structuralize::*;


pub fn parse(input : &str) -> Result<Data, Box<str>> {

    Err("!".into())
}

fn parse_id(input : &mut Chars) -> Result<Data, ParseError> {
   todo!() 
}